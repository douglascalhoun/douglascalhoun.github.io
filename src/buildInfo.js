/**
 * Bumped on each combat/controls deploy so players can verify cache bust.
 * Also shown in the boot toast + HUD.
 */
export const BUILD_ID = 'single-ball-3cap';
export const BUILD_LABEL = `Space Nova · ${BUILD_ID}`;
